import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#cd177c" },
    secondary: { main: "#dc52a4" },
    dark: { main: "#152236" },
    light: { main: "#ffffff" },
    background: { default: "#f4f4ff" },
    gray: { main: "#bebbcf" },
  },
});

export default theme;
